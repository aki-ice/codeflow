apiVersion: apps/v1
kind: Deployment
metadata:
  name: ci-service
  namespace: codeflow
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ci-service
  template:
    metadata:
      labels:
        app: ci-service
    spec:
      containers:
        - name: ci-service
          image: codeflow/ci-service:dev
          imagePullPolicy: IfNotPresent
          command: ["uvicorn", "ci_service.main:app", "--host", "0.0.0.0", "--port", "8000"]
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: codeflow-config
            - secretRef:
                name: codeflow-secrets
          env:
            - name: DATABASE_URL
              value: "postgresql+asyncpg://codeflow:codeflow@postgres:5432/codeflow_ci"
            - name: CI_SIMULATE
              value: "true"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ci-service
  namespace: codeflow
spec:
  selector:
    app: ci-service
  ports:
    - port: 8000
      targetPort: 8000
